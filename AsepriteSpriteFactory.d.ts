export declare class AsepriteSpriteFactory {
    static create(game: Phaser.Game, source: string): Phaser.Sprite;
    static preload(game: Phaser.Game, source: string): void;
    private static getSpritesheetPath(source);
}
